% EARS-MMOEA framework -- hand-designed TikZ vector diagram (no AI image generation).
\begin{tikzpicture}[
  font=\sffamily,
  >={Stealth[length=2.2mm]},
  every node/.style={transform shape},
  box/.style={rounded corners=2pt, draw=black!55, line width=0.5pt,
              align=center, inner sep=3pt, font=\footnotesize},
  io/.style={box, fill=blue!8, draw=blue!45, text width=26mm, minimum height=12mm},
  mod/.style={box, fill=black!4, text width=31mm, minimum height=10mm, font=\scriptsize},
  keybox/.style={box, fill=orange!16, draw=orange!75!red, line width=1pt,
                 text width=44mm, minimum height=15mm},
  arch/.style={box, fill=green!9, draw=green!45!black, text width=29mm, minimum height=10mm,
               font=\scriptsize},
  outb/.style={box, fill=yellow!18, draw=yellow!60!black, text width=26mm, minimum height=13mm},
  eval/.style={box, fill=teal!12, draw=teal!60!black, text width=34mm, minimum height=11mm,
               font=\scriptsize},
  flow/.style={->, draw=black!55, line width=0.7pt},
  back/.style={->, draw=blue!55, line width=0.7pt, dashed},
]

% ---- inputs (left) -------------------------------------------------------
\node[io] (city) {\textbf{Real OpenStreetMap city}\\ street graph,\\ demand nodes};
\node[io, below=6mm of city] (form) {\textbf{MMOP formulation}\\ bi-objective access;\\ multimodal decision space};

% ---- engine modules ------------------------------------------------------
\node[mod, right=15mm of city, yshift=-3mm] (niche) {\textbf{M3} adaptive niching\\(silhouette $k$-means)};
\node[mod, right=7mm of niche] (mate) {\textbf{M4} within-/cross-mode mating\\ \textbf{M6} bandit operators};
\node[mod, below=6mm of niche] (csort) {\textbf{M5} constraint-aware\\ non-dominated sort};
\node[mod, right=7mm of csort] (env) {\textbf{M7} environmental\\ selection};
\node[arch, right=8mm of mate] (arc) {\textbf{M2} three archives\\ Pareto / mode / family};

% highlighted core innovation (M1)
\node[keybox, below=11mm of csort, xshift=18mm] (key)
  {\textbf{M1\ \ Hybrid dual-space key}\\[1pt]
   $D(i)=E(i)\,\bigl(1+\beta\,S(i)\bigr)$\\[1pt]
   {\scriptsize convergence in the \emph{rank}; diversity in a within-rank factor}};

% top spacer to reserve headroom for the feedback arc inside the engine
\node[above=7mm of mate, inner sep=0pt, minimum height=0pt] (pad) {};

% engine background box (drawn behind via fit)
\begin{scope}[on background layer]
  \node[rounded corners=4pt, draw=blue!55, line width=1pt, fill=blue!3,
        fit=(niche)(mate)(csort)(env)(key)(arc)(pad), inner sep=7pt] (engine) {};
\end{scope}
\node[anchor=south, font=\small\bfseries, text=blue!60]
      at ([yshift=2pt]engine.north) {EARS-MMOEA engine\ \ (one generation)};

% ---- outputs / evaluation (right) ---------------------------------------
\node[outb, right=13mm of arc] (fam) {\textbf{Multiple}\\ \textbf{Pareto-equivalent}\\ \textbf{layout families}};
\node[eval, above right=3mm and 7mm of fam, xshift=-3mm] (b1)
   {MMF1--8 benchmark\\ \textbf{rank-1} (avg rank 2.30)};
\node[eval, below right=3mm and 7mm of fam, xshift=-3mm] (b2)
   {5 real OpenStreetMap cities\\ \textbf{rank-1} on the 5-city avg};

% ---- flows ---------------------------------------------------------------
\draw[flow] (city) -- (form);
\draw[flow] (city.east) -- (niche.west);
\draw[flow] (form.east) -- (csort.west);
\draw[flow] (niche) -- (mate);
\draw[flow] (mate) -- (arc);
\draw[flow] (csort) -- (env);
\draw[flow] (env.east) -- (arc.west|-env);
\draw[flow, draw=orange!75!red, line width=1pt] (key.north) -- (env.south)
      node[pos=0.5, right=1pt, font=\scriptsize, text=orange!70!red,
           align=center, text width=24mm] {ranks the \mbox{splitting} front};
\draw[flow] (arc.east) -- (fam.west);
\draw[flow] (fam.east) -- (b1.west);
\draw[flow] (fam.east) -- (b2.west);
% feedback loop: archives -> next-generation population
\draw[back] (arc.north) .. controls +(0,5mm) and +(0,5mm) .. (niche.north)
      node[pos=0.5, font=\scriptsize, text=blue!60, fill=blue!3, inner sep=1pt] {next generation};
\end{tikzpicture}
